#!/bin/sh
# Script to analyse WhatsApp Chat

awk -F ":" '{print $1=$2=""; print $0}' "$1" |
        sed 's/ /\n/g' |
        sed '/^[[:blank:]]*$/d' |
        sort > Words.txt
uniq -c Words.txt | sort -n
#rm Words.txt
